/** @type {import('next').NextConfig} */
const nextConfig: import('next').NextConfig = {
    i18n: {
      locales: ["en", "es"],
      defaultLocale: "es"
    }
  };
  
module.exports = nextConfig;
  